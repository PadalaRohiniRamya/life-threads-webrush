import Icon from"./Icon";
export default function SearchFilters({query,setQuery,source,setSource,type,setType,sort,setSort,total}){return <div className="filters" role="search"><div className="search"><Icon name="Search" aria-hidden="true"/><label className="sr-only" htmlFor="receipt-search">Search receipts</label><input id="receipt-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search artist, track, merchant, category, place…" autoComplete="off"/><button type="button" onClick={()=>setQuery('')} aria-label="Clear search" disabled={!query}><Icon name="X" size={14}/></button></div><label><span className="sr-only">Source</span><select aria-label="Filter by source" value={source} onChange={e=>setSource(e.target.value)}><option value="all">All sources</option><option value="spotify">Spotify</option><option value="household">Household</option><option value="india">Transactions</option></select></label><label><span className="sr-only">Type</span><select aria-label="Filter by type" value={type} onChange={e=>setType(e.target.value)}><option value="all">All types</option><option value="music">Music</option><option value="purchase">Purchases</option></select></label><label><span className="sr-only">Sort</span><select aria-label="Sort receipts" value={sort} onChange={e=>setSort(e.target.value)}><option value="new">Newest</option><option value="old">Oldest</option><option value="amount">Amount</option></select></label><span className="count" aria-live="polite">{total.toLocaleString()} shown</span></div>}
