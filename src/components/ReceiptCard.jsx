import Icon from"./Icon";import{money,formatDate,TYPE_META,SOURCE_META}from"../utils";export default function ReceiptCard({r,onOpen}){const m=TYPE_META[r.type]||TYPE_META.purchase;return <button className="receipt" onClick={()=>onOpen(r)}><div className="receipt-top"><span className={`badge ${m.tone}`}><Icon name={m.icon} size={13}/>{m.label}</span><span className="source">{SOURCE_META[r.source]?.label}</span></div><h3>{r.title}</h3>{r.artist&&<p className="artist">{r.artist}</p>}<p>{r.description||r.album||r.category||r.note||"Recorded digital-life activity."}</p><div className="receipt-foot"><span>{formatDate(r.date)}</span><span>{r.time}</span>{r.amount!=null&&<strong>{money(r.amount)}</strong>}</div></button>}
