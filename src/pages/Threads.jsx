import Icon from"../components/Icon";import{formatDate,money}from"../utils";export default function Threads({threads,onOpen}){return <section className="page"><div className="page-title"><span className="eyebrow">CONNECTION EXPLORER</span><h1>Find the moments<br/>that belong together.</h1><p>Threads are derived in the browser from shared dates, repeated locations and related activity — not invented stories.</p></div><div className="thread-grid big">{threads.map(t=><button className="thread-card" key={t.id} onClick={()=>onOpen(t)}><div className="thread-top"><span>✦ {Math.round(t.score)}% signal</span>{t.date&&<span>{formatDate(t.date)}</span>}</div><h3>{t.title}</h3><p>{t.reason}</p><div className="thread-flow">{t.music.slice(0,2).map(x=><span key={x.id}>🎵 {x.title}</span>)}{t.purchases.slice(0,2).map(x=><span key={x.id}>🛍️ {x.title}</span>)}<Icon name="ArrowRight" size={15}/></div></button>)}</div></section>}
