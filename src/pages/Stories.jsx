import{formatDate,money}from"../utils";import Icon from"../components/Icon";export default function Stories({threads,onOpen}){return <section className="page"><div className="page-title"><span className="eyebrow">STORY MODE</span><h1>From raw data<br/>to a readable story.</h1><p>Open a thread to inspect the exact receipts behind the connection.</p></div><div className="story-list">{threads.slice(0,8).map((t,i)=><button className="story-row" key={t.id} onClick={()=>onOpen(t)}><div className="chapter-num">0{i+1}</div><div><span className="eyebrow">CHAPTER {String(i+1).padStart(2,'0')}</span><h3>{t.title}</h3><p>{t.reason}</p></div><Icon name="ChevronRight"/></button>)}</div></section>}
