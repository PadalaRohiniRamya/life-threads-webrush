import Icon from"../components/Icon";import{money}from"../utils";export default function Insights({stats,insights}){const max=Math.max(...stats.topArtists.map(x=>x[1]),1);return <section className="page"><div className="page-title"><span className="eyebrow">INSIGHTS</span><h1>Patterns, not guesses.</h1><p>These observations are calculated from the uploaded datasets in the browser.</p></div><div className="insight-grid">{insights.map((x,i)=><article className="insight" key={i}><div className="insight-icon"><Icon name={x.icon}/></div><span>{x.metric}</span><h3>{x.title}</h3><p>{x.text}</p></article>)}</div><div className="analytics-two"><div className="panel"><div className="panel-head"><span>Top artists</span><span>Spotify</span></div>{stats.topArtists.slice(0,7).map(([name,n])=><div className="rank" key={name}><div><strong>{name}</strong><small>{n.toLocaleString()} plays</small></div><div className="rankbar"><span style={{width:`${n/max*100}%`}}/></div></div>)}</div><div className="panel"><div className="panel-head"><span>Transaction categories</span><span>All purchases</span></div>{stats.topCategories.slice(0,7).map(([name,n])=><div className="rank" key={name}><div><strong>{name}</strong><small>{n.toLocaleString()} records</small></div><div className="rankbar"><span style={{width:`${n/Math.max(...stats.topCategories.map(x=>x[1]),1)*100}%`}}/></div></div>)}</div></div><div className="data-note"><Icon name="Sparkles"/><div><strong>Transparent by design</strong><p>Cross-dataset “threads” are based on explicit signals such as matching dates or repeated location fields. They are not causal claims.</p></div></div></section>}
