import{useMemo}from"react";import{groupBy}from"../utils";export default function Places({india}){const places=useMemo(()=>{const m=groupBy(india,r=>r.city||r.state||'Unknown');return [...m.entries()].map(([name,rows])=>({name,rows,count:rows.length,total:rows.reduce((a,r)=>a+(r.amount||0),0)})).sort((a,b)=>b.count-a.count).slice(0,20)},[india]);const max=Math.max(...places.map(p=>p.count),1);return <section className="page"><div className="page-title"><span className="eyebrow">PLACES</span><h1>Where the receipts happened.</h1><p>Location fields from the transaction dataset are summarized here. No personal names, card numbers, addresses or dates of birth are displayed.</p></div><div className="places-grid">{places.map(p=><article className="place-card" key={p.name}><div className="place-pin">⌖</div><div><h3>{p.name}</h3><p>{p.count.toLocaleString()} receipts</p><div className="place-bar"><span style={{width:`${p.count/max*100}%`}}/></div></div></article>)}</div></section>}
